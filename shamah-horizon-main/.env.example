# Copy this file to `.env.local` (or set these as environment variables)
# Do NOT commit your real keys. Keep `SUPABASE_SERVICE_ROLE` secret.

# Your Supabase project URL
# - Server usage: SUPABASE_URL
# - Browser (Vite) usage: VITE_SUPABASE_URL
SUPABASE_URL=
VITE_SUPABASE_URL=

# Service role key (server-only). Only use locally for development or in a serverless host.
SUPABASE_SERVICE_ROLE=

# Optional: if you want the browser to use service role for quick local testing (unsafe)
# DO NOT set this in production.
VITE_SUPABASE_SERVICE_ROLE=

# Local or deployed admin endpoint that proxies admin DB writes (recommended)
VITE_ADMIN_INSERT_ENDPOINT=http://localhost:8787/admin/db

# Optional: ONLY if you want the public site to read via the admin endpoint when RLS blocks reads
# (default is off)
VITE_USE_ADMIN_ENDPOINT_FOR_READS=false

# Optional: endpoint that returns signed PUT URLs for byte-level uploads
VITE_SIGNED_UPLOAD_ENDPOINT=

# Optional: anon/public key (used by frontend SDK if needed)
VITE_SUPABASE_ANON_KEY=

# Optional: storage bucket names (must match EXACTLY what you have in Supabase Storage)
# Defaults:
# - VITE_SUPABASE_IMAGES_BUCKET=property-images
# - VITE_SUPABASE_MEDIA_BUCKET=property-media
VITE_SUPABASE_IMAGES_BUCKET=
VITE_SUPABASE_MEDIA_BUCKET=
